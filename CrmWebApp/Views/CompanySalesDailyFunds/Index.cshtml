@model IEnumerable<CrmWebApp.Models.CompanySalesDailyFund>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CompanySalesDailyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SalesSource)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FreezeFund)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkingFund)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NeededFund)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CompanySalesDailyId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SalesSource)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FreezeFund)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkingFund)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NeededFund)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
