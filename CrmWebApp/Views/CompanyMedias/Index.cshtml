@model IEnumerable<CrmWebApp.Models.CompanyMedia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OuterKeyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MediaFor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MediaName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MediaUrl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OuterKeyId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MediaFor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MediaName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MediaUrl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
